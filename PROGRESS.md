# Progress — FigurAction

Last updated: 2026-07-18 (ticket #48 — PWA, clôture Sprint 6)

```
Prompt: Read CLAUDE.md, CODING-PLAN.md and PROGRESS.md. Implement the next uncompleted ticket.
/clear : à chaque nouveau sprint ou si ça déraille
/model : switcher selon l'annotation 〔sonnet〕 ou 〔opus〕 avant chaque ticket
```

---

## Sprint 0 — Fondations

- [x] #01 — Init repo + toutes les dépendances 〔sonnet〕
- [x] #02 — Tailwind + shadcn/ui 〔sonnet〕
- [x] #03 — Structure de dossiers + types de base 〔sonnet〕
- [x] #04 — Biome (linter/formatter) 〔sonnet〕
- [x] #05 — Setup Supabase 〔opus〕 ⚠️ MANUAL (créer projet sur supabase.com, récupérer clés)
- [x] #06 — Setup Drizzle ORM 〔sonnet〕 (DB connectée via session pooler port 5432 — le transaction pooler 6543 ne s'authentifie pas sur ce projet)
- [x] #07 — Providers React (QueryClient + Zustand) 〔sonnet〕
- [x] #08 — Deploy Vercel (staging) ⚠️ MANUAL (connecter repo sur vercel.com)
- [x] #09 — GitHub Actions CI 〔sonnet〕
- [x] #10 — Sentry + PostHog 〔sonnet〕 ⚠️ MANUAL (créer comptes, récupérer clés)

## Sprint 1 — Base de données

- [x] #11 — Tous les schemas Zod 〔sonnet〕
- [x] #12 — Schema DB: profiles + types 〔sonnet〕
- [x] #13 — Schema DB: projects + types 〔sonnet〕
- [x] #14 — Schema DB: castings + types 〔sonnet〕
- [x] #15 — Schema DB: applications + types 〔sonnet〕
- [x] #16 — Schema DB: carpools + contracts + export global 〔sonnet〕
- [x] #17 — RLS Policies 〔opus〕 (helpers use `(select ...)` wrapping; 23/23 isolation assertions pass)
- [x] #18 — Seed data 〔opus〕 (8 users / 3 projects / 6 castings / 10 applications / 3 carpools; auth users via admin API — login `Password123!`; idempotent)

## Sprint 2 — Auth (/clear avant ce sprint)

> **Revue Sprint 2 (2026-07-18)** — correctifs post-review : (1) **[bug HIGH] re-signup avant confirmation** : un 2e signup avec le même email (utilisateur pas encore confirmé) faisait échouer l'insert profil (PK dupliquée) → le "rollback" supprimait le compte auth ORIGINAL et tuait le lien de confirmation ; désormais un 23505 (helper `isUniqueViolation`, remonte la chaîne `cause`) renvoie « compte existe déjà, vérifiez votre boîte mail » SANS supprimer ; les autres échecs gardent le rollback, maintenant best-effort (deleteUser guardé + Sentry). (2) `?next=` préservé à travers login : proxy → `/login?next=<path>` (sauf dashboard), action `login(input, next)` valide via `safeAppPath` (lib/utils — que des chemins `/app/*`, jamais d'open redirect), page login lit le param (useSearchParams sous Suspense, requis par le prerender) ; proxy honore aussi `next` quand un user déjà loggé visite /login. (3) garde `result?.success` sur les actions qui redirect (login, update-password) — ne plus présumer du comportement interne de Next sur les promesses d'actions qui redirigent. (4) `/app/admin` : page placeholder (le nav admin 404ait) ; dirs vides `admin/{parametres,projets,utilisateurs}` + `contrats` supprimés. (5) `/update-password` déplacé dans `(auth)` (réutilise le layout centré) + check session au mount (« lien expiré » immédiat au lieu d'une erreur au submit). (6) listener `onAuthStateChange` unique (AuthInvalidationListener dans QueryProvider) au lieu d'un par consommateur de useCurrentUser. (7) `getAppUrl()` (lib/env) : throw si NEXT_PUBLIC_APP_URL manque au lieu d'emails avec liens "undefined/...". (8) `/app` nu → redirect dashboard ; ui-store purgé de setSidebarOpen/toggleMobileNav (morts). **DÉCISION actée : i18n (next-intl) volontairement différé** — tout le texte UI est en français en dur depuis le Sprint 2 ; à réévaluer avant l'ouverture NL/EN. Non traité (backlog) : rôle dans les claims JWT pour éviter la query rôle par requête dans le proxy.

- [x] #19 — Server action: signup 〔opus〕 (profile insert via Drizzle/service-role — bypasses RLS since no session exists pre-confirmation; rolls back orphaned auth user on failure)
- [x] #20 — Server actions: login + logout 〔opus〕 (server-side redirect on success; generic errors to avoid account enumeration)
- [x] #21 — Server action: reset password 〔opus〕 (resetPassword + updatePassword actions, /auth/callback route exchanges code→session, /update-password page; neutral success to avoid enumeration)
- [x] #22 — Middleware auth + role routing 〔opus〕 (protège /app/*, redirige auth pages si loggé, role gating projets/candidats→production, admin→admin; rôle lu depuis profiles via RLS.)
- [x] #23 — Pages auth UI (login + signup + forgot) 〔opus〕 ((auth) route group + layout centré; react-hook-form + zodResolver; erreurs inline; signup → écran "Vérifiez votre email", forgot → message neutre; les 4 pages rendent 200. Flow submit complet couvert par e2e #51)
- [x] #24 — Hook useCurrentUser 〔opus〕 (TanStack Query; expose user/profile/role/isLoading/isAuthenticated; select supabase alias snake→camel; invalide sur onAuthStateChange. Exercé en live par #25)
- [x] #25 — App shell: sidebar + topbar + mobile nav 〔opus〕 (nav-config partagé filtré par rôle; sidebar desktop collapsible via ui-store; topbar titre + drawer mobile (Sheet Base UI, contrôlé — pas de asChild); bottom tabs mobile; RoleGuard 403. Rendu live via #26)
- [x] #26 — Dashboard 〔opus〕 (page role-aware avec raccourcis cards; 1er consommateur live de #24/#25. FIX: `(app)` route group → segment littéral `app/` — les parens stripaient l'URL donc /app/dashboard 404ait vs le matcher proxy + nav. Vérifié via build + redirect 307)

## Sprint 3 — Profil + Projets + Castings (/clear avant ce sprint)

- [x] #27 — Server actions: profiles 〔opus〕 (updateProfile via client RLS-scoped; uploadPhoto via service-role → path `{userId}/profile.jpg` dérivé de l'user auth (jamais du client), upsert + `?v=` cache-bust; empty strings → null; revalidatePath /app/profil. Bucket `avatars` créé (public read) — upload en service-role donc pas besoin de write policy)
- [x] #28 — Profil figurant: formulaire + upload photo 〔sonnet〕 (profile-form.tsx: RHF+zodResolver, form values typed `z.input<profileSchema>`→submit receives coerced `ProfileInput` since `age` uses z.coerce; photo-upload.tsx: optimistic local object-URL preview, reverts on error; file-upload.tsx: generic label-wrapped hidden-input primitive, reusable. FIX: Base UI's `SelectValue` renders the raw enum value unless given a children render-prop — added one to show French labels. Added `<Toaster />` to root layout (sonner) since no page mounted it yet. Verified live end-to-end via Playwright: login → photo upload → form submit, all confirmed by toasts and persisted values)
- [x] #29 — Profil figurant: page vue + édition 〔sonnet〕 (src/app/app/profil/page.tsx; toggle vue/édition via useState; defaultValues construits depuis useCurrentUser avec isExperienceLevel guard car profile.experience est `string | null` en DB; onSuccess/onUploaded invalident CURRENT_USER_QUERY_KEY pour refléter les changements sans reload. EXPERIENCE_LABELS + isExperienceLevel déplacés de profile-form.tsx vers schemas/profile.ts pour être partagés. Vérifié live via Playwright : login → nav sidebar → vue → édition pré-remplie → save → vue mise à jour + toast)
- [x] #30 — Server actions: projects + castings 〔opus〕 (createProject/createCasting ajoutent un contrôle de rôle explicite — les policies RLS `..._manage_own` ne vérifient que la propriété, pas le rôle, donc un figurant pourrait sinon insérer une ligne dont il est "propriétaire"; update/delete/close s'appuient sur le filtrage RLS silencieux — 0 ligne affectée = accès refusé. FIX découvert en live-testing : `getPublicCastings` renvoyait aussi les castings non-publics du production connecté (RLS `castings_manage_own_project` donne au propriétaire une visibilité totale, indépendamment du statut) — `/app/castings` n'étant pas role-gated, une production pourrait y voir ses propres brouillons ; corrigé par une jointure explicite `projects!inner(status)` indépendante de qui appelle. Nouveau helper partagé `lib/supabase/roles.ts` (dédupliqué depuis proxy.ts). RE-REVIEW opus (le ticket avait été fait en sonnet) → 3 corrections : (1) `data as Project` était doublement faux — la lecture passe par supabase-js qui renvoie du snake_case + timestamps en ISO string, alors que le type Drizzle `Project` est camelCase + `Date` ; ajout de selects aliasés (`PROJECT_SELECT`/`CASTING_SELECT`) façon use-current-user + types `ProjectRow`/`CastingRow` honnêtes ; (2) `updated_at` n'était jamais bumpé sur update (pas de trigger DB, cf #27) — set explicite dans tous les updates ; (3) mapping d'erreur `createCasting` restreint au code RLS 42501. Vérifié en live : sortie camelCase, pas de décalage TZ sur les dates, updated_at bumpé, filtre âge (`.or().or()` = AND) correct — age=20→18-65 only, age=30→les deux, age=70→aucun)
- [x] #31 — Créer un projet (production) 〔sonnet〕 (project-form.tsx : RHF+zodResolver, même pattern `z.input`→`z.output` que ProfileForm pour les champs `z.coerce.date()` ; page `/app/projets/nouveau` avec RoleGuard production+admin — redondant avec le blocage serveur de proxy.ts (déjà vérifié en live : un figurant est redirigé vers /app/dashboard avant même que la page ne rende), mais gardé en défense en profondeur. FIX schéma découvert en live-testing (bloquant pour ce ticket) : `z.coerce.date().optional()` et `z.coerce.number().optional()` ne traitent PAS `""` comme "non renseigné" — un `<input type=\"date\">` ou `type=\"number\"` laissé vide soumet `\"\"`, que `.optional()` ne intercepte pas (il ne saute que `undefined`). Pour les dates ça plantait direct (\"Invalid date\") ; pour les nombres `Number('')` = `0`, qui passait silencieusement si le schéma autorise 0 (ex. `castingSchema.ageMin`) — corruption silencieuse. Ajout de `schemas/shared.ts` (`optionalDate`, `optionalNumber` — préprocessent `\"\"` → `undefined`) appliqué à `project.ts` (dates), `casting.ts` (dates + ageMin/ageMax, pas encore utilisé par une UI mais aurait cassé #33) et rétroactivement à `profile.ts` (age, déjà shippé en #28/#29 — masqué jusqu'ici par le `min(16)` qui transformait le bug en mauvais message d'erreur plutôt qu'en corruption silencieuse). Vérifié en live : soumission avec dates vides → projet créé avec `shootDateStart/End: null` (pas d'erreur, pas de `\"\"`) ; avec dates → valeurs correctes sans décalage TZ ; contrôle DB direct après coup pour confirmer). FIX post-hoc (re-check Sprint 3) : après création, le projet n'apparaissait pas dans la liste — `createProject` fait un `revalidatePath` (cache serveur) mais la liste lit le cache client TanStack Query (staleTime 60s), jamais invalidé ; le projet était bien en DB mais masqué jusqu'à expiration du staleTime. Ajout de `queryClient.invalidateQueries(MY_PROJECTS_QUERY_KEY)` dans project-form onSuccess (comme #29/#33 le faisaient déjà pour leurs queries). Seul createProject manquait cette invalidation.
- [x] #32 — Liste projets + hooks 〔sonnet〕 (use-projects.ts : TanStack Query autour de getMyProjects ; getMyProjects étendu avec un count agrégé PostgREST `castings(count)` plutôt qu'un fetch séparé par projet — expose `ProjectListItem.castingsCount` ; project-card.tsx : badge de statut, plage de dates formatée en manipulant directement la string `YYYY-MM-DD` (pas de `Date`/`Intl` — évite tout risque de décalage TZ comme repéré en #30/#31) ; page `/app/projets` : filtre par statut en state local (pas de store partagé, un seul consommateur), grid, empty state par statut, lien "Nouveau projet" stylé via `buttonVariants` sur `Link` plutôt qu'un `render` prop Base UI non éprouvé ailleurs dans le code. Vérifié en live : compteurs de castings corrects (2/2/0), dates sans décalage, filtres + empty state, boucle complète création→liste (le redirect de #31 vers /app/projets qui 404ait resout maintenant), figurant toujours redirigé par le rôle-gate serveur)
- [x] #33 — Détail projet + créer casting 〔opus〕 (page `/app/projets/[id]` client (useParams) : détail projet + grid castings + Dialog contrôlé "Ajouter un casting" ; nouvelle action `getProject(id)` scopée owner (`.eq(production_id)` + maybeSingle) plutôt que RLS seul — sinon `projects_select_open` laisserait une prod ouvrir le projet ouvert d'une autre prod dans cette vue de gestion ; hooks `useProject`/`useProjectCastings`, invalidation castings + MY_PROJECTS (le compteur de la liste) après ajout/fermeture ; casting-form.tsx (RHF+zodResolver, même z.input→z.output), casting-card.tsx (badge, places, tranche d'âge, "Fermer le casting" conditionnel aux ouverts). ROLE_TYPE_LABELS partagé ajouté à schemas/casting.ts. FIX découvert en live-testing (console error) : le Select roleType passait uncontrolled→controlled (field.value undefined au 1er render) → warning Base UI ; corrigé avec `value={field.value ?? null}` (null = placeholder tout en restant contrôlé), + même correctif rétroactif sur profile-form.tsx (experience null possible). Vérifié en live : nav liste→détail, ajout casting via dialog→grid mis à jour, fermeture→badge Fermé + bouton disparaît, toasts, 0 erreur console)

## Sprint 4 — Candidatures (/clear avant ce sprint)

> **Revue Sprint 4 (2026-07-17)** — vérif fonctionnelle + sécurité de #34→#40. RLS write-guards OK en live (figurant ne peut pas confirmer, prod ne peut pas postuler, self-confirm bloqué, isolation cross-prod OK). 3 correctifs appliqués : (1) **[bug] piège withdraw→re-postuler** : après un retrait, la contrainte UNIQUE(casting,figurant) verrouillait le figurant à vie — `createApplication` ressuscite désormais une candidature `withdrawn` (update→pending, reset reviewed_at/by) au lieu de ré-insérer ; `ApplicationForm` ne bloque plus sur `withdrawn` et affiche une note « vous pouvez postuler à nouveau ». (2) **[bug UX] filtre lieu** : `.trim()` à chaque frappe empêchait de taper une localité multi-mots (ex. « La Louvière ») — la valeur brute est conservée, le trim est fait dans le hook avant la query. (3) **[durcissement] fiche candidat** : `?application=` forgé pointant vers une autre candidature → `getApplicationReviewInfo` renvoie `figurantId`, la page ne montre les boutons review que s'il matche l'id de l'URL (l'écriture restait de toute façon RLS-gated). **Risque de sécurité accepté (design #17, hors scope Sprint 4)** : n'importe quelle production peut lire le profil COMPLET (email + téléphone) de n'importe quel figurant, même non-postulant (`profiles_select_figurants_by_staff` + feature « Browse all candidates » du CLAUDE.md). Intentionnel produit mais sensible RGPD (UE/Belgique) — à durcir plus tard (restreindre aux figurants ayant postulé, ou masquer le tél. avant candidature). Autre point noté (non corrigé) : `applications_update_own_figurant` autorise un figurant à repasser une candidature `rejected`/`confirmed` en `pending` via l'API brute (pas d'escalade — il ne peut jamais se `confirmed` lui-même) — intégrité mineure, hérité #17.

- [x] #34 — Catalogue castings public (SSR/SEO) 〔sonnet〕 (`(public)/castings/page.tsx` Server Component réutilisant `getPublicCastings()` déjà écrit en #30 — pas de nouvelle query ; `components/castings/casting-card.tsx` nouveau (distinct du `casting-card.tsx` de gestion sous `projets/`, qui a un bouton "Fermer" et pas de lien) — prend un `href` fourni par l'appelant plutôt que de décider lui-même de la destination, pour être réutilisable tel quel par #35 (catalogue authentifié) ; la page vérifie `supabase.auth.getUser()` côté serveur pour choisir le CTA de chaque card : `/login` si non connecté, `/app/castings/[id]` si connecté (cette dernière route n'existe pas encore — livrée par #36, lien correct dès maintenant). Metadata SEO (title/description/OG) statique sur la page. Pas de page `(public)/castings/[id]` créée — hors périmètre du ticket, la CTA renvoie vers l'espace authentifié plutôt que vers une fiche publique. Vérifié en live : build + `pnpm check` propres, route `/castings` bien listée en ƒ (dynamic/SSR) dans l'output de build, fetch non-authentifié → 200, 2 castings ouverts affichés (les 4 autres du seed sont closed/draft ou sous projet non-open), titres/lieu/date/tranche d'âge/places rendus correctement, liens pointent vers `/login` en session anonyme)
- [x] #35 — Catalogue castings authentifié + filtres 〔opus〕 (`app/castings/page.tsx` client, réutilise le `CastingCard` de #34 tel quel (href `/app/castings/[id]` — la fiche+form de candidature arrivent en #36 ; pas de bouton "Postuler" séparé, la card entière EST la CTA, cohérent avec #34 et pour ne pas front-runner l'action `createApplication` de #36). Pas de RoleGuard : `/app/castings` est volontairement ouvert à tout authentifié (cf. rationale #30 — c'est pourquoi `getPublicCastings` a son inner-join `projects!inner(status)` indépendant de l'appelant, pour qu'une production n'y voie pas ses brouillons) ; le nav ne l'expose qu'aux figurant/admin mais la route reste accessible en direct. Pas de h1 dans la page — la topbar dérive déjà le titre "Castings" de nav-config. FIX (bloquant, découvert au build) : `use-castings.ts` existait déjà (créé en #33 : `useProjectCastings`/`projectCastingsQueryKey`, consommés par `projets/[id]`) — mon Write l'a d'abord écrasé, cassant le build ; restauré depuis git + fusionné avec le nouveau `useCastings(filters)`. Store filtres (`filters-store.ts`) : remplacé les placeholders `ageMin`/`ageMax` par un seul `age` (l'âge du figurant) — mapper une plage sur le filtre d'inclusion d'âge de `getPublicCastings` (garde les castings dont [ageMin,ageMax] accepte cette valeur) n'aurait aucun sens ; `casting-filters.tsx` : lieu (ilike), type de rôle (Select Base UI, sentinelle `"all"`→null, même render-prop SelectValue que casting-form), âge (number, garde `Number.isFinite` sinon null), date de tournage, + "Réinitialiser" conditionnel ; les filtres nourrissent la query key TanStack donc refetch auto par combinaison. Vérifié en live contre la DB seed (client anon, RLS expose les castings open comme prouvé en #34) : no-filter→2, location=Bruxelles→2, roleType=figurant→2, age=20→1 (seul le casting 18-65 ; le 25-55 exclu correctement), age=70→0 ; build + `pnpm check` propres, `/app/castings` bien listée, accès non-auth → 307 vers /login via proxy)
- [x] #36 — Postuler à un casting 〔opus〕 (page `app/castings/[id]/page.tsx` en Server Component (async `params` Next 16) : `getCastingDetail(id)` (nouvelle action dans castings.ts) → `notFound()` si null, + `getMyApplication(id)` pour l'état "déjà postulé" ; détail casting + bloc projet parent. `getCastingDetail` embarque le projet via `projects!inner(...)` — le `!inner` réutilise la RLS `projects_select_open` pour ré-imposer que le projet soit ouvert (un casting sous projet draft/closed n'est jamais postulable ici, quel que soit l'appelant) ; cast `as unknown as` car supabase-js (schéma non typé) infère l'embed to-one comme un array alors qu'au runtime c'est un objet. `actions/applications.ts` (nouveau) : `createApplication` prend `figurant_id` de la session (jamais du client) + status 'pending' (imposé aussi par la RLS `applications_insert_own_figurant` qui vérifie en plus le rôle figurant) ; garde explicite "casting ouvert" via lecture RLS-scoped (la policy insert ne checke PAS le statut du casting) ; contrainte UNIQUE(casting_id, figurant_id) = backstop anti-doublon race-safe → 23505 mappé "déjà postulé", 42501 → "seuls les figurants". `application-form.tsx` (client) : profil pré-rempli en lecture seule via useCurrentUser (nom/âge/ville/expérience — EXPERIENCE_LABELS+isExperienceLevel de #29), message libre optionnel (maxLength 2000), gate rôle≠figurant → message, état `appliedStatus` local pour flip immédiat sans reload + revalidatePath sur `/app/candidatures` et la fiche. Décision : pas de composant status-badge partagé ici (livré par #37) — libellé de statut inline. Vérifié en live contre la DB seed avec une VRAIE session figurant (signInWithPassword, RLS active) : insert valide (own id, pending) → OK ; 2e insert → bloqué 23505 ; insert forcé status=confirmed → bloqué 42501 (défense RLS confirmée, en plus du fait que l'action force pending) ; embed projet parent OK ("Les Ombres de Bruxelles") ; cleanup service-role. Build + `pnpm check` propres, `/app/castings/[id]` listée en ƒ, accès non-auth → 307 /login. NB: lucas.dubois avait déjà postulé aux 2 castings ouverts du seed → test basculé sur emma.lambert. Le lien depuis le catalogue #35 (`/app/castings/[id]`) résout désormais.)
- [x] #37 — Mes candidatures (figurant) 〔sonnet〕 (`use-applications.ts` (`useMyApplications`), `shared/status-badge.tsx` (ApplicationStatus → libellé+variant+icône lucide : pending outline/Clock, confirmed default/CheckCircle2, rejected destructive/XCircle, withdrawn secondary/Ban — partagé, réutilisé par la prod en #38/#39), `shared/empty-state.tsx` (générique icône+titre+description+action, remplace les empty-states inline), page `app/candidatures/page.tsx` (liste de cards : titre casting, lieu, date, StatusBadge ; "Retirer" seulement si status=pending → `withdrawApplication` + invalidation TanStack ; empty-state avec CTA vers /app/castings). Actions ajoutées à `applications.ts` : `getMyApplications` + `withdrawApplication`. DÉCISION DE SÉCURITÉ/VISIBILITÉ (le point non-trivial de ce ticket) : `getMyApplications` lit via le client SERVICE-ROLE scopé strictement à `figurant_id = user.id`, PAS via RLS. Raison : la policy `castings_select_open` ne montre que les castings open dans un projet open ; or un figurant doit continuer à voir un casting auquel il a postulé même après sa fermeture — sinon l'embed `castings(...)` revient NULL et la ligne perd son titre/lieu/date. Le filtre `figurant_id` (jamais une valeur client) EST la frontière de sécurité, même pattern que uploadPhoto (#27). `withdrawApplication` reste sur le client RLS (la policy `applications_update_own_figurant` scope + plafonne le nouveau statut à pending/withdrawn) + filtre `figurant_id` en défense en profondeur. Le titre n'est un lien vers `/app/castings/[id]` que si le casting est encore open (sinon texte simple — évite un lien mort vers la fiche #36 qui ne rend que les castings open). Cast `as unknown as` sur l'embed to-one (supabase-js l'infère array). Vérifié en live contre la DB seed : (1) test construit — application d'un figurant sur "Danseurs de rue" (casting open MAIS projet draft) : vue service-role → casting visible, vue client-RLS → casting NULL (masqué par castings_select_open) → PROUVE la nécessité du service-role ; (2) withdraw via RLS → OK status=withdrawn ; cleanup service-role. Les 2 candidatures existantes du seed pour lucas pointaient toutes vers des castings open (pas de cas fermé sous la main → d'où le test construit). Build + `pnpm check` propres, `/app/candidatures` listée, accès non-auth → 307 /login.)
- [x] #38 — Liste candidats par casting (production) 〔opus〕 (page `app/projets/[id]/candidats/page.tsx` (RoleGuard production/admin, useParams) : `getProjectCandidates(projectId)` (nouvelle action dans projects.ts) → candidats groupés PAR CASTING (`groupByCasting`), sélection multiple d'IDs d'applications remontée à la page (`Set<string>` + toggle/toggleGroup) pour que la barre d'actions bulk de #39 puisse agir cross-casting ; compteur "X sélectionné(s)". `candidate-list.tsx` (une section par casting : checkbox "sélectionner tout" avec état indeterminate, lignes avatar+nom+âge·ville+StatusBadge, nom cliquable → `/app/candidats/[figurantId]` fiche #40). Nouveau composant UI `ui/checkbox.tsx` (wrap Base UI Checkbox — pas de shadcn ; l'Indicator générique de Base UI n'accepte PAS de children-fonction contrairement à SelectValue, donc icônes Check/Minus pilotées par CSS via `group-data-[checked]`/`group-data-[indeterminate]` sur la Root + keepMounted). Lien "Voir les candidats" ajouté sur la fiche projet #33 pour la découvrabilité. SÉCURITÉ : `getProjectCandidates` ne fait PAS de contrôle d'ownership explicite — il s'appuie sur la RLS `applications_select_production` (owns_casting) qui ne renvoie que les candidatures des castings possédés, + `profiles_select_figurants_by_staff` pour lire les profils figurants ; une prod passant le projectId d'une autre prod obtient une liste vide. `castings!inner` scope au projet ; hint `profiles!figurant_id` désambiguïse les 2 FK applications→profiles (figurant_id vs reviewed_by) — testé : `profiles!applications_figurant_id_fkey` échoue (mauvais nom auto-généré), `profiles!figurant_id` OK. Withdrawn exclus (non actionnables). Cast `as unknown as` sur les embeds to-one (supabase-js les infère array). Vérifié en live avec une VRAIE session production (rtbf.productions, RLS active) : projet "Les Ombres de Bruxelles" → 6 candidats sur 2 castings, profils complets (nom/âge/ville) + statuts mixtes pending/confirmed/rejected ; ISOLATION prouvée → requête sur le projet "Nuit Blanche" d'une autre prod = 0 ligne (RLS seule). Build + `pnpm check` propres, `/app/projets/[id]/candidats` listée en ƒ, accès non-auth → 307 /login (préfixe proxy `/app/projets` couvre aussi le role-gate figurant→redirect).)
- [x] #39 — Confirmer / Refuser un candidat 〔opus〕 (actions dans `applications.ts` : `confirmApplication`/`rejectApplication`/`bulkUpdateApplications` délèguent toutes à un `reviewApplications(ids, status)` interne partagé — set status + `reviewed_by`=user.id + `reviewed_at`=now + `updated_at` explicite (pas de trigger DB, cf. #27/#30). SÉCURITÉ : PAS de contrôle d'ownership explicite — la RLS `applications_update_production` (owns_casting) plafonne l'update aux candidatures des castings possédés, donc les ids non-possédés sont filtrés silencieusement (`updated` = nb réellement modifié). `ReviewStatus = Extract<ApplicationStatus,'confirmed'|'rejected'>` (jamais pending/withdrawn côté prod). Hook `useReviewApplications(projectId)` (useMutation, OPTIMISTIC : onMutate flip le status dans le cache `projectCandidatesQueryKey`, rollback via contexte `previous` on error, invalidate on settle ; mutationFn throw si `!success` pour déclencher le rollback ; toast succès pluralisé selon `updated`). UI `candidate-list.tsx` : boutons Confirmer (vert Check) / Refuser (rouge X) par ligne, affichés conditionnellement (Confirmer caché si déjà confirmed, Refuser caché si déjà rejected → permet de corriger une décision) ; page candidats : barre d'actions groupée quand sélection non vide (Confirmer/Refuser la sélection via `[...selectedIds]`), `handleReview` retire les ids traités de la sélection on success. La sélection multi-casting câblée en #38 est maintenant consommée. Vérifié en live avec une VRAIE session production (rtbf, RLS active, snapshot+restore) : confirm single → 1 ligne, status=confirmed, reviewed_by bien stampé ; bulk reject → 2 lignes rejected ; ISOLATION → update d'une candidature d'une AUTRE prod = 0 ligne modifiée + reviewed_by resté null (RLS seule). Build + `pnpm check` propres, `/app/projets/[id]/candidats` en ƒ, accès non-auth → 307 /login.)
- [x] #40 — Fiche profil candidat (production) 〔sonnet〕 (page `app/candidats/[id]/page.tsx` client (RoleGuard production/admin + proxy gate `/app/candidats`) : grande photo (Avatar lg + fallback initiales), infos (âge/ville/téléphone/expérience), bio, bouton "Contacter" (mailto avec le VRAI email du profil), boutons Confirmer/Refuser. DÉCISION CLÉ : l'URL est keyée par figurant (`/app/candidats/[figurantId]`) mais un figurant peut avoir plusieurs candidatures → confirm/refuser seraient ambigus. Résolu en faisant porter l'id de candidature par un query param : le lien de #38 est passé à `/app/candidats/${figurantId}?application=${candidate.id}` ; la page lit `?application` via useSearchParams et n'affiche les boutons review + le badge de statut + le titre du casting QUE si présent. Nouvelles actions : `getFigurantProfile(id)` (profiles.ts, RLS `profiles_select_figurants_by_staff` + `.eq('role','figurant')`) et `getApplicationReviewInfo(id)` (applications.ts, RLS `applications_select_production`/owns_casting → sert aussi de contrôle d'ownership : renvoie null si la prod ne possède pas le casting, donc les boutons review n'apparaissent jamais pour une candidature non-possédée). Hooks `use-candidates.ts` (`useFigurantProfile` + `useApplicationReview` avec `enabled: id!==null`). Confirm/refuser réutilisent `confirmApplication`/`rejectApplication` de #39 en direct (pas d'optimistic ici — pas de liste partagée sur cette page, juste invalidation de `applicationReviewQueryKey` + toast) ; boutons désactivés si déjà dans l'état cible. Vérifié en live avec session production (rtbf, RLS) : getFigurantProfile → OK (Lucas, email réel, age) ; getFigurantProfile(id d'une production) → null (pas un figurant) ; reviewInfo(candidature possédée) → OK status+casting ; reviewInfo(candidature d'une autre prod) → null (owns_casting). Build + `pnpm check` propres, `/app/candidats/[id]` en ƒ, accès non-auth → 307 /login. ⇒ Sprint 4 (candidatures) terminé.)

## Sprint 5 — Emails + Covoit (/clear avant ce sprint)

- [x] #41 — Setup Resend + templates email 〔sonnet〕 ⚠️ MANUAL (créer compte Resend, API key, domaine)
- [x] #42 — Brancher les emails sur les actions 〔opus〕 (couche d'envoi + branchements aux 3 moments : signup→welcome, review→confirmation/refus, convocation manuelle. ARCHITECTURE SÉCURITÉ (point non-trivial) : les senders bas niveau vivent dans `lib/resend/send.ts` (PAS un "use server") — chacun prend une adresse `to` brute, donc les exposer comme Server Actions serait un relais ouvert (phishing depuis le domaine). Ils sont appelés uniquement par du code serveur de confiance déjà autorisé. `server-only` n'est pas installé dans ce repo (ni utilisé par admin.ts) → garde par doc-comment comme admin.ts plutôt qu'ajouter une dep. Tous les senders avalent leurs erreurs (best-effort, `Sentry.captureException`) et renvoient `{success}` sans throw : un email qui échoue ne casse jamais l'action déclencheuse (un candidat confirmé reste confirmé). BRANCHEMENTS : (1) `signup` → `sendWelcomeEmail` après l'insert profil (welcome N'EST PAS une action exposée — pas de session à ce stade pré-confirmation, donc un `sendWelcome({to})` public serait un relais ouvert ; d'où l'appel direct à la lib). (2) confirmation/refus : le branchement est mis dans `reviewApplications` (le helper interne partagé) et PAS dans confirmApplication/rejectApplication — car les 3 chemins UI (boutons ligne + bulk via `bulkUpdateApplications`, ET la fiche candidat #40 via confirm/reject) transitent tous par lui ; le mettre ailleurs raterait le chemin bulk qui est le principal. `notifyReviewOutcome` ne notifie QUE les ids réellement modifiés (`updated`, post-RLS) puis lit email+contexte casting/projet via la MÊME session RLS (owns_casting + staff read). (3) `actions/emails.ts` = `sendConvocation(input)`, seule vraie Server Action (seul flux déclenché par le client) : liste de destinataires dérivée 100% serveur (jamais du client) — projet lu scopé `production_id=user.id` (ownership), figurants confirmés lus via RLS, dédupliqués par email (un figurant confirmé sur 2 castings du projet = 1 seul mail). UI : `ConvocationDialog` (Dialog + RHF + zodResolver(convocationSchema) nouveau schéma ; DialogTrigger `render`-prop Base UI comme #33) sur la page candidats, bouton "Envoyer une convocation" ; toast pluralisé + cas `sent===0` → warning "aucun figurant confirmé". Helper `formatDateFr` (Intl fr-BE, timeZone UTC pour ne pas décaler une date-only) ajouté à lib/utils, utilisé dans confirmation + convocation. ENVOI RÉEL TESTÉ ✓ : RESEND_API_KEY réelle configurée ; aucun domaine vérifié (`/domains`=[]), donc `RESEND_FROM=OnSet <onboarding@resend.dev>` ajouté à .env.local (sender partagé Resend — en mode test ne délivre qu'à l'email owner du compte). Envoi live du template welcome → `onset.app.dev@gmail.com` (= email owner du compte Resend) ACCEPTÉ (message id retourné, pas d'erreur). ⚠️ À FAIRE PLUS TARD : vérifier le domaine onset.app dans Resend puis repasser `RESEND_FROM` (ou le défaut) à `noreply@onset.app` pour envoyer à n'importe quel destinataire (les seed figurants @example.be seront rejetés en mode test tant que onboarding@resend.dev est le sender). VÉRIFIÉ : build + tsc + `pnpm check` propres ; les 2 nouvelles requêtes PostgREST testées EN LIVE avec une vraie session production (rtbf, RLS active) → shapes exactes attendues, embeds to-one résolus en objets (confirme les casts `as unknown as`) ; render des templates confirmation+convocation avec données réelles → HTML OK, date FR "10 septembre 2026" sans décalage TZ.)
- [x] #43 — Covoiturage: CRUD complet 〔sonnet〕 (`actions/carpools.ts` : createCarpool (driver_id de la session jamais du client — vérifié par `carpools_insert_own` ; date normalisée YYYY-MM-DD via toDateString), markCarpoolFull/deleteCarpool (s'appuient sur le filtrage RLS silencieux `carpools_update_own`/`carpools_delete_own` — 0 ligne = pas le propriétaire → OWNERSHIP_ERROR), getCarpools (lisible par tout authentifié via `carpools_select_all` ; embed `projects(title)` qui ne résout que pour les projets open donc un covoit lié à un projet non-open n'affiche pas de nom — acceptable), getOpenProjects (projets open pour le select du form, via `projects_select_open`). `use-carpools.ts` : useCarpools + useOpenProjects. `carpool-form.tsx` : RHF+zodResolver, même split z.input→z.output que les autres forms (departureDate coerce.date, seatsAvailable coerce.number) ; projet optionnel via Select sentinelle `"none"`→undefined ; contactMethod Select (render-prop SelectValue comme casting-form) ; driverName pré-rempli depuis le profil. `CONTACT_METHOD_LABELS` ajouté à schemas/carpool.ts (partagé). `carpool-list.tsx` : filtres date (`>=` sur string YYYY-MM-DD) + projet (options dérivées des covoits chargés, pas de requête séparée) en state local ; cards (conducteur, projet, lieu, date via formatDateFr + heure sliced HH:MM, places, badge Complet/places) ; bouton Contacter = `<a>` mailto/tel stylé via buttonVariants (Button Base UI n'a pas d'asChild) ; boutons "Marquer complet" (caché si déjà complet) + "Supprimer" conditionnels au propriétaire (driverId === currentUserId de useCurrentUser). Page `app/covoiturage/page.tsx` : RoleGuard figurant+admin (cohérent avec nav-config ; covoiturage pas gaté dans proxy donc RoleGuard = la frontière côté client) ; form (dans une Card) + liste ; markFull/delete câblés au niveau page (toast + invalidation CARPOOLS_QUERY_KEY, pendingId pour désactiver pendant l'appel). VÉRIFIÉ EN LIVE (vraies sessions figurant, RLS active) : getCarpools 3 lignes shape camelCase correcte + embed projet objet/null (confirme cast `as unknown as`), time "08:00:00" géré par slice ; getOpenProjects 1 projet ; create/markFull(own)/delete(own) → 1 ligne chacun ; ISOLATION lucas→covoit d'emma : markFull ET delete → 0 ligne (RLS). Build + tsc + `pnpm check` propres, `/app/covoiturage` listée. ⇒ Sprint 5 terminé.)

## Sprint 6 — Landing + SEO + PWA (/clear avant ce sprint)

- [x] #44 — Landing page 〔opus〕 (la page "coming soon" de `src/app/page.tsx` est REMPLACÉE par une vraie landing déplacée dans `src/app/(public)/page.tsx` (résout toujours `/`, option prévue par le plan) pour hériter du nouveau `(public)/layout.tsx` : `SiteHeader`/`SiteFooter` partagés (components/layout/) — réutilisables tels quels par #45/#46, et la page `/castings` de #34 gagne au passage le chrome public (elle n'avait AUCUNE nav pour revenir à l'accueil). Sections : hero (badge, headline, 2 CTA figurant→/signup + production→/pour-les-productions, lien /castings), problème/solution (2 colonnes X/Check), 4 features (icônes lucide), comment ça marche (3 étapes + lien productions), 3 témoignages PLACEHOLDERS (personas fictifs — à remplacer par de vrais retours), CTA final (bande primary), footer 4 colonnes (liens, contact mailto, mentions légales). Animation subtile : `animate-in fade-in slide-in-from-bottom-4` (tw-animate-css, déjà importé) sur le hero uniquement — pas de scroll-triggered (aurait exigé du JS client, page 100% Server Component/SSG). Liens forward vers `/devenir-figurant` + `/pour-les-productions` (livrées #45/#46 — même pattern que #34→#36) ; `/mentions-legales` créée en page minimale (noindex, éditeur/contact/hébergement/RGPD à compléter avec l'identité légale avant lancement) pour ne pas laisser un lien footer mort non planifié ailleurs. FIX au passage : `globals.css` hardcodait `font-family: Arial` (reliquat scaffold) alors que Geist était chargé et exposé en variables depuis #01 sans jamais être appliqué — mapping `--font-sans`/`--font-mono` ajouté au `@theme inline` + body sur `var(--font-sans)` (typo app-wide passe Arial→Geist). Metadata title/description/OG propres sur la landing (le gros œuvre SEO reste #47). Vérifié : build + tsc + `pnpm check` propres, `/` désormais ○ statique (SSG) dans l'output de build, serveur `next start` → 200 sur `/`, `/castings`, `/mentions-legales`, toutes les sections + title/OG présents dans le HTML servi, header/footer bien rendus sur /castings)
- [x] #45 — Page B2B: Pour les productions 〔sonnet〕 (`(public)/pour-les-productions/page.tsx` Server Component 100% statique (○ SSG), hérite du chrome SiteHeader/SiteFooter via le `(public)/layout.tsx` de #44 — le lien forward du hero landing + nav header résout désormais. Sections : hero B2B (CTA /signup + mailto contact), argumentaire 3 pain points barrés → gains (grid 2 col, pattern avant/après distinct du 2-colonnes de la landing pour varier), 6 features pro (grid lg:3 col — features RÉELLEMENT livrées uniquement : projets/castings #30-33, profils #40, tri bulk #38-39, emails #42, convocations #42, covoiturage #43 vendu comme anti-no-show) + encart "Bientôt" bordure dashed (call-sheets/e-signature/recherche avancée = phase 2 du CLAUDE.md — séparés des features actuelles pour ne pas survendre), 3 étapes production, PRICING PREVIEW honnête (décision : pas de tarifs inventés — Stripe/monétisation = phase 2 ticket #12 ; card "Lancement 0€" (badge "Disponible maintenant", CTA signup) + card "Pro — Bientôt" grisée (CTA mailto "être prévenu") avec mention explicite que les tarifs seront annoncés avant toute facturation), CTA final bande primary. Metadata title/description/OG propres. Vérifié : `pnpm check` + build propres, `/pour-les-productions` ○ statique dans l'output, `next start` → 200, toutes les sections + title présents dans le HTML servi)
- [x] #46 — Page SEO: Devenir figurant 〔sonnet〕 (`(public)/devenir-figurant/page.tsx` Server Component 100% statique (○ SSG), chrome public hérité de #44 — dernier lien forward de la nav/landing qui 404ait, tout le site public résout désormais. Guide evergreen ciblant "devenir figurant belgique"/"casting figurant bruxelles" (title, description, h1, villes citées Bruxelles/Liège/Namur/Charleroi/Mons/Gand/Anvers) : hero avec CTA /signup + /castings, section définition "qu'est-ce qu'un figurant" en prose + 4 cards "pourquoi", guide numéroté 5 étapes (ol sémantique) calqué sur le vrai flow produit (#28 profil → #35 filtres → #36 candidature → #42 emails/convocation), 6 conseils de sélection, FAQ 7 questions en `<details>`/`<summary>` natifs (pas de JS client, page reste full server) + CTA final. STRUCTURED DATA : JSON-LD FAQPage (schema.org) injecté en `<script type="application/ld+json">` — seul usage légitime de dangerouslySetInnerHTML (contenu 100% statique depuis les constantes du module, aucune donnée user — conforme à la règle CLAUDE.md qui ne l'interdit qu'avec des données user ; biome-ignore documenté). Prudence factuelle : rémunération présentée comme variable selon production (défraiement/cachet, "précisé dans l'annonce"), âge min 16 ans aligné sur le `min(16)` du profileSchema, mise en garde contre les agences payantes. FIX process de vérif : le `pkill -f "next start"` des tickets précédents ne tuait PAS le serveur (pattern ≠ nom du process node) → le serveur de #45 squattait :3111 et servait l'ancien build (404 trompeur sur la nouvelle route) ; tué par PID via `lsof -tnP -iTCP:3111`, à réutiliser pour les prochains tickets. Vérifié : `pnpm check` + build propres, `/devenir-figurant` ○ statique, 200 servi, toutes les sections + title + JSON-LD FAQPage présents dans le HTML)
- [x] #47 — Meta tags + Open Graph + sitemap 〔sonnet〕 (DÉVIATION assumée : pas de `next-sitemap` (le plan datait d'avant) — conventions natives App Router `app/sitemap.ts` + `app/robots.ts`, zéro dépendance (respecte la règle #01 "plus d'install en milieu de sprint"). Root layout : `metadataBase` depuis NEXT_PUBLIC_APP_URL, title template `%s | ONSET` + default, openGraph siteName/locale fr_BE/type, twitter summary_large_image — les titres des 5 pages publiques réalignés sur le template (suffixes "| ONSET" manuels retirés ; landing en `title.absolute` pour garder son titre brandé). Image OG par défaut : `app/opengraph-image.tsx` en ImageResponse (next/og) 1200×630 générée AU BUILD — pas d'asset binaire à produire/committer, PNG 38KB servi, alt exporté ; s'applique à toutes les routes (+ twitter:image auto). GOTCHA découvert en vérif live : le merge des metadata Next est SHALLOW par clé top-level — un `openGraph:` défini par une page écrase ENTIÈREMENT celui hérité (og:image du file-convention, site_name, locale disparaissaient sur les pages #44-46 qui dupliquaient title/description dans openGraph) ; corrigé en SUPPRIMANT les blocs openGraph par page (Next auto-dérive og:title/og:description du title/description résolus — vérifié tag par tag). METADATA DYNAMIQUES /castings/[id] : le plan les prévoyait mais #34 n'avait pas créé la fiche publique → créée maintenant `(public)/castings/[id]/page.tsx` (réutilise `getCastingDetail` de #36 dont les guards open-casting/open-project sont indépendants de l'appelant — fonctionne en anonyme via RLS), `generateMetadata` (title "titre — casting à lieu", description tronquée 160c, canonical) + `cache()` React pour dédupliquer le fetch metadata/page, notFound() sinon ; CTA auth-aware (/app/castings/[id] si loggé, /signup sinon + lien login). Le catalogue public #34 pointe désormais l'anonyme vers `/castings/[id]` (crawlable, chaque casting = page indexable) au lieu de /login. `sitemap.ts` : 5 routes statiques + 1 entrée par casting ouvert (lastModified=updatedAt, ƒ dynamic → les nouveaux castings apparaissent sans redeploy) ; `robots.ts` : disallow /app/, /auth/, pages auth, /api/ + lien sitemap. ⚠️ NEXT_PUBLIC_APP_URL doit être configurée sur Vercel avec le vrai domaine (sinon les URLs absolues sitemap/canonical/OG pointent localhost). Vérifié en live (seed) : robots.txt + sitemap.xml servis (2 castings seed dedans), fiche publique 200 avec title dynamique "Passants pour scène de marché — casting à Bruxelles | ONSET" + canonical, id inconnu → 404, og:image présent sur les 5 pages publiques + fiche, /opengraph-image 200 image/png ; build + `pnpm check` propres)
- [x] #48 — PWA: manifest + service worker + install 〔sonnet〕 (PIVOT D'INTÉGRATION découvert au build : le mode webpack-plugin classique de `@serwist/next` (withSerwist dans next.config) est un NO-OP silencieux sous Turbopack — le bundler des builds Next 16 — public/sw.js n'était jamais généré (seul un warning noyé dans le log le disait). Passé en "configurator mode" (voie stable bundler-agnostic recommandée par Serwist, vs `@serwist/turbopack` encore expérimental) : `serwist.config.mjs` (swSrc src/sw.ts → swDest public/sw.js) + build script `next build && serwist build serwist.config.mjs` (le CLI ne cherche que serwist.config.js par défaut → chemin passé en argument ; deps ajoutées : @serwist/cli + esbuild en dev, sanctionné par le plan qui prévoyait l'install ici) ; ce mode tourne APRÈS le build Next donc précache automatiquement toutes les routes prerendered (66 URLs / 3.22 MB, dont /offline) ; l'enregistrement du SW passe par `<SerwistProvider swUrl="/sw.js" disable={NODE_ENV!=="production"}>` dans le root layout (plus d'auto-register bundler ; désactivé en dev où sw.js n'existe pas et empoisonnerait le HMR). Manifest : convention native `app/manifest.ts` (→ /manifest.webmanifest auto-linké, cohérent #47) — name/short_name, start_url /app/dashboard, display standalone, theme #0a0a0a, lang fr. ICÔNES 192/512 SANS ASSET BINAIRE : routes `/icon-192` + `/icon-512` (route handlers force-static → ○ au build) rendues par `lib/pwa-icon.tsx` en ImageResponse — wordmark "ON" centré dans la safe-zone 80% donc la même image sert purpose any ET maskable ; même philosophie que l'OG image #47. Stratégies de cache : `defaultCache` de @serwist/next/worker (static cache-first, data network-first, pages SWR — couvre le plan) + fallback document → /offline précaché. Page `/offline` (server + RetryButton client location.reload, noindex) ; `install-banner.tsx` (client, root layout) : beforeinstallprompt (Chromium-only) → bannière fixed bottom Installer/Fermer, preventDefault contre la mini-infobar, refus mémorisé en localStorage (`onset-install-banner-dismissed`), masquée après appinstalled. tsconfig lib += webworker ; .gitignore += public/sw.js* (généré au build — Vercel exécute le script build donc le SW sort en prod sans commit). Viewport themeColor ajouté au root layout. Vérifié en live (next start) : /sw.js 200 js, /manifest.webmanifest 200 (contenu complet), icônes 200 image/png (2.9KB/12.6KB), /offline 200, link rel=manifest + meta theme-color dans le head, build + `pnpm check` propres. ⚠️ MANUAL restant : test d'install réel sur Android + test offline dans un navigateur (nécessite un device/DevTools — le SW ne s'active qu'en HTTPS ou localhost). ⇒ Sprint 6 terminé.)

## Sprint 7 — Tests (/clear avant ce sprint)

- [ ] #49 — Setup Vitest + tests schemas 〔sonnet〕
- [ ] #50 — Setup Playwright 〔sonnet〕
- [ ] #51 — Test e2e: auth flow 〔opus〕
- [ ] #52 — Test e2e: flow candidature 〔opus〕
- [ ] #53 — Test e2e: flow production 〔opus〕

---

## Légende

- 〔sonnet〕 = ticket mécanique, bien défini → `/model sonnet`
- 〔opus〕 = ticket complexe, logique métier, sécurité, multi-fichiers → `/model opus`
- ⚠️ MANUAL = action manuelle requise sur un dashboard externe
- (/clear avant ce sprint) = faire `/clear` dans Claude Code avant d'attaquer

Après chaque ticket terminé : cocher [x], mettre à jour la date "Last updated".
